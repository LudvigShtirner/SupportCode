import XCTest
@testable import SupportCodePackage

final class SupportCodePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SupportCodePackage().text, "Hello, World!")
    }
}
